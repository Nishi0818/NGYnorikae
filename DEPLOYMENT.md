# GitHub と Vercel への公開手順

このリポジトリは、名古屋市営地下鉄の時刻表と乗換ロジックをブラウザ内で処理する**静的ウェブアプリ**としてVercelへ公開できる構成です。Vercel用の設定は `vercel.json` に含まれ、ビルド時は `pnpm run build:web` によりExpo Webの静的ファイルを `dist/` へ出力します。

## GitHub への反映

GitHub連携を有効にした後、対象のGitHubリポジトリへこのプロジェクトを反映します。GitHub上で既存リポジトリを使う場合は、Vercel側でも同じリポジトリを接続してください。

## Vercel での公開

Vercelのダッシュボードで対象GitHubリポジトリを **Import** し、ルートディレクトリをリポジトリ直下のままとします。`vercel.json` がビルドコマンド、出力先、pnpmによる依存関係インストールを指定するため、追加の環境変数は不要です。Import後に **Deploy** を実行すると、以後はGitHubの既定ブランチへの更新が自動デプロイされます。

## 公開前チェック

`pnpm run build:web` で静的ビルドが成功することを確認し、生成される `dist/` をVercelが配信します。時刻表データ、乗換時間、Noto Sans JPのサブセット、PWAアセットはすべてリポジトリに含まれるため、経路検索の主要機能は外部APIを必要としません。
