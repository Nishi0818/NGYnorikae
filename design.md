# なごや地下鉄オフライン案内 — モバイルUI設計

## 設計方針

本アプリは、通信が不安定な地下空間でも片手で素早く次の列車を判断できることを目的とする。画面は常に縦長の **9:16** を前提に、主操作を親指の届きやすい画面下半分へ集約する。採用する方針は、**駅構内クイック検索を主軸に、iOSらしい簡潔な情報整理を組み合わせること**である。出発駅・到着駅・「次の列車を探す」を最優先で表示し、日時・平日／土休日は「出発：いま」の要約行から必要時だけ開く補助設定に退避する。経路結果では、まず次の発車時刻、行先、到着時刻、乗換可否を一目で判断できるようにする。

## カラーと視覚言語

白を主役にした余白のある情報設計を維持し、面には半透明の白、極細の青系境界線、淡い青の影を用いる控えめなガラス調を採用する。基調面は白 **#FFFFFF**、本文は墨色 **#37312B**、操作ボタンは地下鉄を想起させる深い青 **#0B5B95** とする。アプリアイコンには紫・黄・赤・青の路線色を交差する路線として取り込み、検索ボタンには青い列車アイコンと黄の細い縁を使う。出発マーカーと経路区間は、選択・検索された実際の路線色を使う。色の役割を路線識別と操作の焦点に限定し、白いガラス面の可読性を保つ。

## 画面構成

| 画面 | 主な内容 | 主な操作 |
| --- | --- | --- |
| 検索ホーム | コンパクトなオフライン状態、出発駅・到着駅の連結ルート入力、現在時刻の要約、最近の検索、補助設定への入口 | 駅の選択、駅の入替、履歴の再利用・削除、次の列車の検索、出発条件の調整 |
| 駅選択シート | ひらがな・漢字・路線名での検索候補、最近使った駅、全駅一覧 | 駅の確定、候補の絞り込み |
| 経路結果 | 最短と乗換少なめを比較し、選択した経路の次の発車時刻、到着時刻、所要時間、乗換回数、乗換に使える時間を表示 | 比較軸の切替、経路詳細の展開、検索条件の修正 |
| 経路詳細 | 区間ごとの発時刻・着時刻、路線色、乗換に必要な時間、注意表示 | 表示の展開・折りたたみ |
| 設定・データ情報 | 収録データの改定日、休日判定の説明、オフライン使用状況、データ更新方法 | 情報の確認、データ更新手順の参照 |

## 主な利用フロー

ユーザーは検索ホームで出発駅を選択し、到着駅を選択した後、画面下寄りの「次の列車を探す」を押す。日時は初期状態で「いま」として扱い、平日／土休日の調整が必要なときだけ要約行を押して補助設定を開く。検索後は、最も早い列車の発車時刻・行先・到着時刻を先頭に提示し、その下で乗換駅と乗換に必要な時間を確認できる。

もう一つの重要な流れは、通信がない状態での利用である。時刻表・駅・路線・乗換条件はアプリに同梱するため、初回インストール完了後は検索に通信を要求しない。画面上部には小さなデータ状態表示を設け、時刻表の収録基準日と、リアルタイム運行情報は対象外である旨を誤解なく示す。

## iOSらしい操作感とアクセシビリティ

画面の見出しは大きめのセマンティックな文字サイズを使い、入力欄と主要ボタンのタップ領域は少なくとも44pt相当を確保する。駅選択と経路詳細は下から現れるシートとして扱い、検索実行など即時に完了する操作には過度なアニメーションを使用しない。路線色だけでは情報を伝えず、必ず路線名・方向・駅名を併記する。運休・データ範囲外などの注意は赤だけに頼らず、明確な文言とアイコンを組み合わせる。
